# DLSS5 NR 运行库准备

## 使用方式

选择 **自动下载原版** 时，插件在首次打开页面时从已配置来源准备缺少的运行库，显示下载进度。
不需要手动找下载地址、填写文件路径或勾授权确认框。下载失败可以点击顶部的
**重试运行库并检测显卡**，成功后按显卡型号选择即可。
缺少依赖时，展开 **高级：诊断与修复**，点击 **修复依赖并继续**。

文件保存在 **Forge模型目录 / DLSS-NR**：

```text
models/
	DLSS-NR/
		nvngx_dlssnr.dll
		caller/
			nvngx.dll_comfy.dll
```

Forge使用自定义模型目录时，同样跟随它，不写死盘符。第一项是NVIDIA运行库，第二项是MIT桥接辅助组件。
插件自己的Python依赖和原生桥仍留在扩展目录，不会替换Forge的PyTorch环境。

## 默认下载来源

已从原下载记录追溯到：
[Magpie v0.6.6-experimental](https://github.com/SAOG0721/Magpie/releases/tag/v0.6.6-experimental)，
文件为[DLSSNR-DLL-Options-310.8.0.0.zip](https://github.com/SAOG0721/Magpie/releases/download/v0.6.6-experimental/DLSSNR-DLL-Options-310.8.0.0.zip)。

该包同时包含原版与社区替代文件。自动模式下载整包，但**只解出**`NVIDIA-Original/nvngx_dlssnr.dll`，
不解出或执行社区成员。已重新实测下载，原版NVIDIA签名有效，与原使用文件逐字节一致。
这是**第三方分发来源**，不是NVIDIA官方下载地址或背书；本项目没有把该DLL转存到自己的仓库。
插件安装器另行从本项目GitHub Release获取MIT桥接组件。

## 手动／社区版

顶部选择 **手动自定义／社区版**，再到 **高级：诊断与修复** 中选文件并点 **导入运行库副本**。
也可把自己的`nvngx_dlssnr.dll`放到界面显示的目录，默认`models/DLSS-NR/manual`，再重试准备。
辅助组件会放在旁边，不需要覆盖自动原版。

手动模式刷新或重启后仍保留；缺文件时**绝不自动下载原版兜底**，不替换用户文件，也不声称通过NVIDIA签名。
只检查本地Windows x64 DLL文件头和复制完整性，这不是安全扫描；请仅使用可信文件。
40系／社区版的实际生成兼容性尚未在本项目实测，导入成功不能代替出图验证。

切换或导入前须确认本插件后台空闲。源文件不被修改，目标已有不同文件时保留旧件；
需要手工替换时先停止使用该运行库。切回自动原版使用另一目录，手动副本仍保留。

## 检查与重试

- 普通运行库重试不执行依赖安装；只有明确点击修复依赖才重装依赖。
- 依赖修复仅关闭本插件的空闲私有后台；NR正在处理或后台状态未知时拒绝修复。
- 顶部显示简短的中文原因与下一步，完整错误在高级选项的诊断框中。
- 自动下载先写临时文件，完成并通过NVIDIA签名检查后才发布为正式文件；中断或失败不留下假成品。
- 不设置NVIDIA运行库的固定hash或版本白名单。SHA-256只用于复制完整性和本机文件身份。
- 已有有效运行库会直接复用，不会每次重新下载，也不会自动替换成另一个版本。
- 文件就绪不等于GPU兼容。第一次真实生成仍可能报告驱动或运行库接口不兼容；不会把原图当作增强成功。
- 不要为解决错误关闭安全软件、修改驱动或删除整个模型目录。

## 发布者配置

下载地址由项目维护者在`runtime-download.json`配置，不是最终使用者的设置。
`url`填写固定的公开HTTPS文件地址；直下DLL时`member`为null，ZIP时填写包内确切的`nvngx_dlssnr.dll`路径。
当前URL直接指向上面的第三方Release资产，`member`为`NVIDIA-Original/nvngx_dlssnr.dll`。
来源记录和有效厂商签名不等于再分发授权；本项目没有向自己的GitHub或Hugging Face上传NVIDIA文件。

MIT桥的下载摘要只校验本项目组件，不代表NVIDIA运行库版本锁定。NVIDIA软件的许可不由本项目的MIT许可证授予。